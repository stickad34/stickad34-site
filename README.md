# Stickad 34 Montpellier — site vitrine (one-page)

Contenu :
- `index.html` : le site complet (HTML + CSS + JS intégré)
- `robots.txt`, `sitemap.xml` : bases SEO
- `assets/` : mets ici tes images (réalisations, logo...)

## Mise en ligne (le plus simple)
1) Ouvre `index.html` sur ton ordinateur pour tester (double-clic).
2) Envoie `index.html` + `robots.txt` + `sitemap.xml` + `assets/` sur ton hébergement (OVH/o2switch/etc).
3) Le site est en ligne.

## À personnaliser vite fait
- Dans `index.html`, cherche `EMAIL_DEST` (déjà réglé sur yilmaz@stickad.fr) ou change-le si besoin.
- Ajoute ton téléphone dans la section "Infos" (ou dis-moi ton tel/email et je te le remets propre).
- Remplace la section "Réalisations" par des images (je peux te faire une grille + lightbox si tu veux).

Bon lancement 🚀


## Images services
Les images des services sont dans `assets/service-*.svg`. Remplace-les par tes vraies photos en gardant les mêmes noms.

- `assets/service-cartes-flyers.svg` (cartes/flyers)
- `assets/service-logos-maquette.svg` (logo/mise en page)
- `assets/service-baches.svg` (bâches)
